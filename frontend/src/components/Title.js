import React from 'react'

function Title({ title }) {
    return (
        <div>
            <h1 className='my-4 display-5 text-start border-bottom p-3'>{title}</h1>
        </div>
    )
}

export default Title
